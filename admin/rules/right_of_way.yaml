metadata:
  title: right_of_way
state_machine:
  direction: LR
  initial_state: idle
  states:
  - idle
  - intersection
  - warning
  - violation
  transitions:
  - from: '[*]'
    to: idle
  - from: idle
    to: intersection
    condition: in_intersection == True and ego_has_stop_sign == False and ego_has_red_light == False
  - from: intersection
    to: warning
    condition: right_wedge_far == True
  - from: warning
    to: intersection
    condition: right_wedge_far == False
  - from: warning
    to: violation
    condition: right_wedge_near == True
  - from: violation
    to: idle
    condition: in_intersection == False
  - from: intersection
    to: idle
    condition: in_intersection == False
  - from: warning
    to: idle
    condition: in_intersection == False